{
  "name": "mumbai-insider",
  "version": "0.2.0",
  "private": true,
  "description": "Mumbai Insider — customer-journey prototype (static) + Supabase backend plan.",
  "scripts": {
    "dev": "npx serve public -l 3000",
    "build": "echo 'Static site — no build step. Vercel will serve /public directly.'",
    "start": "npx serve public"
  },
  "engines": {
    "node": ">=18"
  }
}
